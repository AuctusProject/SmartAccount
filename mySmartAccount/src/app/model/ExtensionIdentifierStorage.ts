export class ExtensionIdentifierStorage {
    name: string;
    identifier: string;

    constructor(name?: string, identifier?: string){
        this.name = name;
        this.identifier = identifier;
    }
}